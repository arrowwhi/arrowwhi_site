{{define "base"}}
<!doctype html>
<html lang='en'>
    <head>
        <meta charset='utf-8'>
        <title>{{ .title }}</title>
    </head>
    <body id = "main_body">
        {{template "header" .}}
            {{template "main" .}}
        {{template "footer" .}}
    </body>
</html>
{{end}}
